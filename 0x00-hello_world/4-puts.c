#include <stdio.h>
/**
 * main - program starting point
 *
 * return: by default returns zero if no error, else non-zero value is returned
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
