#include <stdio.h>
/**
 * main - program starting point
 *
 * return: zero  if no error, else non-zero
 */

int main (void)
{
	puts("Programming is like building a multilingual puzzle");

	return (0);

}
